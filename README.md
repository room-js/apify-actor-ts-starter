## Starter template for Apify Actor

### Tech

- TypeScript
- Biome.js
- NPM package manager
- Docker

### Development

You can create `.env.local` file in the root folder and place there environment variables for the local use.

Use `npm run dev` command to run the actor locally.

### Keep in mind

Don't forget to adjust the image in the `Dockerfile` to match your needs.
